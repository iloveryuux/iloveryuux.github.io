---
import TimelineItem from '@/ui/TimelineItem.astro'

const items = [
  {
    title: 'Personal ecosystem of tools',
    description:
      'The project includes linters, commit conventions, loggers, and validators to enhance productivity and code quality in large projects.'
  }
]
---

<ol class="relative border-s-2 mt-10">
  {items.map(item => (
    <TimelineItem title={item.title} description={item.description} />
  ))}
</ol>
